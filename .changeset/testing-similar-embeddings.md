---
"@nestjs-adk/testing": major
---

Add lexical `await using` cleanup for `AdkTestBed`, add the `toBeSimilarTo` embedding matcher and remove `ApiKeyGate`; `RunTranscript` now labels users, agents, tool requests and responses, transfers, delegations, approvals, and rejections; paid suites now load environment variables in their test configuration and fail normally when required credentials are absent.
